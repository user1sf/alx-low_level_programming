#include <stdio.h>
/**
 *main - Entry point
 *Doc: alx task
 *Return: Always 0 (Success)
 */
int main(void)

{
	int i;

	for (i = '0' ; i <= '9' ; i++)
	{
		putchar(i);
	}
	putchar(10);
	return (0);
}
